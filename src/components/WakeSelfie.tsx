  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

E  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

F  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

@  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

-  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

W  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

W  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

@  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

-  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

*  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

*  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

L  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

-  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

W  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

U  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

A  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

M  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

N  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

*  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

W  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

D  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

C  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

D  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

E  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

W  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

C  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

F  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

W  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

H  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

T  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

M  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

L  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

V  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

E  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

M  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

[  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

M  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

]  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

A  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

X  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

E  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

N  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

q  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

'  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

I  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

[  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

]  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

[  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

B  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

]  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

E  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

!  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

D  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

?  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

U  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

M  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

E  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

D  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

U  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

M  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

M  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

6  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

!  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

T  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

E  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

O  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

j  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

M  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

C  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

A  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

?  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

T  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

E  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

[  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

]  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

!  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

!  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

B  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

E  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

8  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

M  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

*  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

H  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

6  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

W  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

8  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

C  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

?  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

I  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

D  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

U  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

L  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

j  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

7  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

z  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

I  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

D  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

T  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

F  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

O  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

Z  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

B  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

6  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

z  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

?  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

T  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

E  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

D  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

M  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

M  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

T  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

B  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

-  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

-  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

W  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

U  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

A  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

M  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

C  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

C  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

z  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

I  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

9  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

9  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

9  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

9  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

I  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

8  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

C  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

P  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

3  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

6  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

%  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

6  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

5  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

5  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

6  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

9  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

5  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

A  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

I  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

w  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

%  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

3  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

j  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

F  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

X  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

-  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

#  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

S  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

z  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

3  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

m  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

8  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

C  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

C  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

5  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

5  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

4  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

C  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

C  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

!  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

=  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

x  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

R  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

W  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

7  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

8  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

5  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

3  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

(  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

5  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

2  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

5  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

0  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

,  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

p  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

!  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

r  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

|  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

?  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

.  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

6  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

1  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

{  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

y  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

?  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

C  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

h  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

c  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

g  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

…  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

:  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

T  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

a  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

k  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

s  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

l  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

f  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

e  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

"  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

b  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

u  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

t  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

o  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

n  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

<  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

/  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

d  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

i  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

v  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

>  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

   const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

)  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

;  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

}  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };


  const grab = (v: HTMLVideoElement, w: number) => {
    const c = document.createElement("canvas");
    c.width = w; c.height = Math.round(w * (v.videoHeight || 640) / (v.videoWidth || 480));
    const ctx = c.getContext("2d"); ctx?.drawImage(v, 0, 0, c.width, c.height);
    return { c, ctx };
  };
  const grey = (v: HTMLVideoElement) => {
    const { c, ctx } = grab(v, 32);
    const px = ctx?.getImageData(0, 0, c.width, c.height).data ?? new Uint8ClampedArray();
    const out: number[] = []; for (let i = 0; i < px.length; i += 4) out.push((px[i] + px[i + 1] + px[i + 2]) / 3);
    c.width = 0; return out;
  };
  const jpeg = (v: HTMLVideoElement) => { const { c } = grab(v, 480); const d = c.toDataURL("image/jpeg", 0.7); c.width = 0; return d; };

  const capture = async () => {
    const v = videoRef.current;
    if (!v || !ready || busy) return;
    setBusy(true);
    setMsg("Hold still and look at the camera…");
    let a = jpeg(v); let b = "";
    // Liveness: ~2 s of 32x32 greyscale samples; a live face shows small natural motion.
    let prev = grey(v); let total = 0; let n = 0;
    for (let i = 0; i < 13; i++) {
      await new Promise(r => setTimeout(r, 150));
      const cur = grey(v);
      let d = 0; for (let k = 0; k < cur.length; k++) d += Math.abs(cur[k] - (prev[k] ?? 0));
      total += d / Math.max(1, cur.length); n++; prev = cur;
    }
    const motion = total / Math.max(1, n);
    if (motion < 0.6) { a = ""; setMsg("No natural movement detected. Use your live face, not a photo."); setBusy(false); return; }
    b = jpeg(v);
    try {
      const tz = Intl.DateTimeFormat().resolvedOptions().timeZone;
      const r = await verify({ data: { frames: [a, b], motion, tz } });
      a = ""; b = "";
      if (r.ok) { streamRef.current?.getTracks().forEach(t => t.stop()); onDone(r); return; }
      setMsg(r.message);
    } catch { setMsg("Selfie check failed. Try again."); }
    finally { a = ""; b = ""; setBusy(false); }
  };

